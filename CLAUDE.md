# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Git Configuration

- **GitHub user**: cytt861
- **Remote**: `git@github.com:cyt3112268880-maker/git.git`
- **Branch**: `main`
- **Auth**: SSH key at `~/.ssh/id_ed25519`

## Quick Git Commands

```bash
git ship "message"   # Alias that runs: git add . → git commit → git push (all in one)
```

You can also use the standard Git workflow if you need more control:
```bash
git add .
git commit -m "message"
git push
```
